# Examples

`compose.smoke.yaml` is the local appliance smoke (DEP-001):
`--netconf-listen=:1830 --restconf-listen=:8303`, `cap_drop: ALL`,
testdata lab host key. SWAP-001 adds `labnetconf.yaml`, Jungle
registration, and labinfo stubs referenced by
[docs/13](../docs/13-integration-lab-swap.md).
