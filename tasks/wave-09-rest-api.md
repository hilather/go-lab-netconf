# API-001 — REST /v1

Status: not-started
Depends: APP-001
Owns: internal/control/rest, api/openapi

## Goal
Every PARITY_REQUIRED REST row. problem+json. health unauthenticated.

## Non-scope
Auth enforcement beyond a stub (SEC-001), MCP, UI.

## Tests
Contract tests for datastore get/set/commit, notifications wait, state export YAML.
