# Contributing

Read `START-HERE.md` and `AGENTS.md`. Take one wave from
`tasks/00-program-board.md`. Docs ship with the change.

Node **22.14.0** and npm ≥10.9.0 for `web/` (`make web-install web-test web-build`).
